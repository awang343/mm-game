[package]
name = "mm-server"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mm-server"
path = "src/main.rs"

[dependencies]
tiny_http = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
dirs = "5"

[profile.release]
lto = "thin"
strip = true
